<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: Arial, sans-serif; background: #f4f6f9; margin: 0; padding: 20px; }
    .container { max-width: 560px; margin: 0 auto; background: #ffffff; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 12px rgba(0,0,0,0.08); }
    .header { background: linear-gradient(135deg, #0F172A 0%, #1E3A5F 100%); padding: 28px 32px; }
    .header h1 { color: #38BDF8; font-size: 22px; margin: 0; }
    .header p { color: #94A3B8; font-size: 13px; margin: 4px 0 0; }
    .body { padding: 32px; }
    .subject { font-size: 18px; font-weight: 700; color: #0F172A; margin-bottom: 12px; }
    .message { font-size: 15px; color: #374151; line-height: 1.6; background: #F8FAFC; border-left: 4px solid #38BDF8; padding: 14px 18px; border-radius: 6px; }
    .footer { padding: 16px 32px; background: #F8FAFC; border-top: 1px solid #E5E7EB; font-size: 12px; color: #9CA3AF; text-align: center; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>BugZera</h1>
      <p>Platform Notification</p>
    </div>
    <div class="body">
      <div class="subject"><%= @subject %></div>
      <div class="message"><%= @body %></div>
    </div>
    <div class="footer">
      This is an automated notification from BugZera &mdash; <%= Time.current.strftime('%d %b %Y, %H:%M') %>
    </div>
  </div>
</body>
</html>
